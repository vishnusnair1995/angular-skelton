import { RevrseStrigPipe } from './revrse-strig.pipe';

describe('RevrseStrigPipe', () => {
  it('create an instance', () => {
    const pipe = new RevrseStrigPipe();
    expect(pipe).toBeTruthy();
  });
});
