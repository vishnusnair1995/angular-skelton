import { RevrseStrigDirective } from './revrse-strig.directive';

describe('RevrseStrigDirective', () => {
  it('should create an instance', () => {
    const directive = new RevrseStrigDirective();
    expect(directive).toBeTruthy();
  });
});
